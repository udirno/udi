#get ship as a list of tuples based on where the user wants to place them
    #generate a list of tuples
#the board is just a 2d list
    #when user wants to place vertical only have to change the first number of the tuple
    #for horizontal, change only the second number of the tuple
def player_names(players: Iterable["Player"]) -> str: )

    :
while (True):
    p1_name = input("Enter Player one name:")
    p2_name = input("Enter Plater two name:")
    if p1_name == p2_name or p1_name == '' or p2_name == '':
        print('Please enter a different name:')
    else:
        return p1_name, p2_name
        break